# merge-back

Makes a branch from main, rebases with develop and creates a PR targeting develop. PR doesn't have any approvers. CODEOWNERS should work, but it's not.

Now we use git commands to do what we want!
